diff --git a/core/assets/scaffold/files/default.services.yml b/core/assets/scaffold/files/default.services.yml
index 45d986c..53267eb 100644
--- a/core/assets/scaffold/files/default.services.yml
+++ b/core/assets/scaffold/files/default.services.yml
@@ -53,7 +53,8 @@ parameters:
     # For more information about debugging Twig templates, see
     # https://www.drupal.org/node/1906392.
     #
-    # Not recommended in production environments
+    # Use true in production environments.
+    # False is not recommended in production environments.
     # @default false
     debug: false
     # Twig auto-reload:
@@ -62,7 +63,7 @@ parameters:
     # If you don't provide a value for auto_reload, it will be determined
     # based on the value of debug.
     #
-    # Not recommended in production environments
+    # Null is not recommended in production environments.
     # @default null
     auto_reload: null
     # Twig cache:
@@ -73,7 +74,8 @@ parameters:
     # auto_reload setting above should be enabled rather than disabling the
     # Twig cache.
     #
-    # Not recommended in production environments
+    # Use false in production environments.
+    # True is not recommended in production environments.
     # @default true
     cache: true
   renderer.config:
@@ -124,7 +126,8 @@ parameters:
   # For more information about debugging cacheable responses, see
   # https://www.drupal.org/developing/api/8/response/cacheable-response-interface
   #
-  # Not recommended in production environments
+  # Use true in production environments.
+  # False is not recommended in production environments.
   # @default false
   http.response.debug_cacheability_headers: false
   factory.keyvalue: {}
diff --git a/sites/default/default.services.yml b/sites/default/default.services.yml
index 45d986c..53267eb 100644
--- a/sites/default/default.services.yml
+++ b/sites/default/default.services.yml
@@ -53,7 +53,8 @@ parameters:
     # For more information about debugging Twig templates, see
     # https://www.drupal.org/node/1906392.
     #
-    # Not recommended in production environments
+    # Use true in production environments.
+    # False is not recommended in production environments.
     # @default false
     debug: false
     # Twig auto-reload:
@@ -62,7 +63,7 @@ parameters:
     # If you don't provide a value for auto_reload, it will be determined
     # based on the value of debug.
     #
-    # Not recommended in production environments
+    # Null is not recommended in production environments.
     # @default null
     auto_reload: null
     # Twig cache:
@@ -73,7 +74,8 @@ parameters:
     # auto_reload setting above should be enabled rather than disabling the
     # Twig cache.
     #
-    # Not recommended in production environments
+    # Use false in production environments.
+    # True is not recommended in production environments.
     # @default true
     cache: true
   renderer.config:
@@ -124,7 +126,8 @@ parameters:
   # For more information about debugging cacheable responses, see
   # https://www.drupal.org/developing/api/8/response/cacheable-response-interface
   #
-  # Not recommended in production environments
+  # Use true in production environments.
+  # False is not recommended in production environments.
   # @default false
   http.response.debug_cacheability_headers: false
   factory.keyvalue: {}
